namespace ClassManagement.Domain.Modules.Admin.Enums;

// In-app notification lifecycle state (MVP-7). Stored as PascalCase text.
public enum NotificationStatus
{
    Unread,
    Read,
    Archived,
}
